# ─── Render (backend + worker) ──────────────────────
# Neon Postgres (free): https://neon.tech
DATABASE_URL=postgres://user:pass@host.neon.tech/plugin_monitor?sslmode=require
# Upstash Redis (free): https://upstash.com — use native Redis endpoint (not HTTP)
REDIS_URL=rediss://default:password@host.upstash.io:6379
JWT_SECRET=change_me
PLUGIN_SECRET=pm_shared_secret_change_in_production
FRONTEND_URL=https://plugin-monitor.vercel.app

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ─── Vercel (frontend) ─────────────────────────────
VITE_API_URL=https://plugin-monitor-api.onrender.com
